{
  "name": "fake-products-api",
  "version": "1.0.0",
  "description": "Simple Express.js API & static JSON generator for GitHub Pages",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "node scripts/build.js",
    "lint": "echo 'Running lint checks... (No linter configured)'",
    "test": "echo 'Running test cases... (No tests configured)'",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [
    "express",
    "mock",
    "api",
    "github-pages",
    "json"
  ],
  "author": "Mooz AI",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "nodemon": "^3.1.0"
  }
}
